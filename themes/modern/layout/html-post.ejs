<!DOCTYPE html>
<html lang="<%= config.language %>">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= page.title %> | <%= config.title %></title>
  <meta name="description" content="<%= page.excerpt || page.description || config.description %>">
  <meta name="keywords" content="<%= (page.tags && page.tags.length ? page.tags.map(tag => tag.name).join(',') : config.keywords) %>">
  <meta name="author" content="<%= config.author %>">
  
  <!-- Open Graph -->
  <meta property="og:title" content="<%= page.title %>">
  <meta property="og:description" content="<%= page.excerpt || page.description || config.description %>">
  <meta property="og:type" content="article">
  <meta property="og:url" content="<%= config.url + page.permalink %>">
  
  <!-- 主题样式 -->
  <link rel="stylesheet" href="<%= url_for('css/style.css') %>">
  
  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  
  <!-- 数学公式支持 -->
  <% if (theme.math || page.math) { %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/katex@0.16.9/dist/katex.min.css">
    <script defer src="https://cdn.jsdelivr.net/npm/katex@0.16.9/dist/katex.min.js"></script>
    <script defer src="https://cdn.jsdelivr.net/npm/katex@0.16.9/dist/contrib/auto-render.min.js"></script>
  <% } %>
  
  <style>
    /* HTML文章专用样式 */
    .html-post-container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 2rem;
      background: white;
      min-height: 100vh;
    }
    
    .html-post-header {
      text-align: center;
      margin-bottom: 2rem;
      padding-bottom: 1rem;
      border-bottom: 2px solid #e2e8f0;
    }
    
    .html-post-title {
      font-size: 2.5rem;
      font-weight: 700;
      color: #1e293b;
      margin-bottom: 1rem;
    }
    
    .html-post-meta {
      display: flex;
      justify-content: center;
      gap: 2rem;
      color: #64748b;
      font-size: 0.9rem;
    }
    
    .html-post-meta i {
      margin-right: 0.5rem;
    }
    
    .html-post-content {
      line-height: 1.7;
      color: #374151;
    }
    
    .html-post-tags {
      margin-top: 2rem;
      padding-top: 1rem;
      border-top: 1px solid #e2e8f0;
      text-align: center;
    }
    
    .html-post-tag {
      display: inline-block;
      background: #f1f5f9;
      color: #475569;
      padding: 0.25rem 0.75rem;
      border-radius: 12px;
      font-size: 0.8rem;
      margin: 0.25rem;
      text-decoration: none;
      transition: all 0.3s;
    }
    
    .html-post-tag:hover {
      background: #2563eb;
      color: white;
    }
    
    .back-to-home {
      position: fixed;
      top: 2rem;
      left: 2rem;
      background: rgba(37, 99, 235, 0.9);
      color: white;
      padding: 0.75rem 1rem;
      border-radius: 8px;
      text-decoration: none;
      font-weight: 500;
      transition: all 0.3s;
      z-index: 1000;
    }
    
    .back-to-home:hover {
      background: rgba(37, 99, 235, 1);
      transform: translateY(-2px);
    }
    
    @media (max-width: 768px) {
      .html-post-container {
        padding: 1rem;
      }
      
      .html-post-title {
        font-size: 2rem;
      }
      
      .html-post-meta {
        flex-direction: column;
        gap: 0.5rem;
      }
      
      .back-to-home {
        top: 1rem;
        left: 1rem;
        padding: 0.5rem 0.75rem;
        font-size: 0.9rem;
      }
    }
  </style>
</head>
<body>
  <!-- 返回首页按钮 -->
  <a href="<%= url_for('/') %>" class="back-to-home">
    <i class="fas fa-home"></i> 返回首页
  </a>
  
  <div class="html-post-container">
    <!-- 文章头部信息 -->
    <header class="html-post-header">
      <h1 class="html-post-title"><%= page.title %></h1>
      <div class="html-post-meta">
        <span><i class="fas fa-calendar"></i> <%= date(page.date, 'YYYY年MM月DD日') %></span>
        <% if (page.updated && page.updated.toString() !== page.date.toString()) { %>
          <span><i class="fas fa-edit"></i> <%= date(page.updated, 'YYYY年MM月DD日') %></span>
        <% } %>
        <% if (page.categories && page.categories.length > 0) { %>
          <span><i class="fas fa-folder"></i> <%= page.categories.map(cat => cat.name).join(', ') %></span>
        <% } %>
      </div>
    </header>
    
    <!-- HTML文章内容 -->
    <main class="html-post-content">
      <%- page.content %>
    </main>
    
    <!-- 标签 -->
    <% if (page.tags && page.tags.length > 0) { %>
      <footer class="html-post-tags">
        <% page.tags.forEach(function(tag) { %>
          <a href="<%= url_for(tag.path) %>" class="html-post-tag">
            <i class="fas fa-tag"></i> <%= tag.name %>
          </a>
        <% }) %>
      </footer>
    <% } %>
  </div>
  
  <!-- 数学公式渲染 -->
  <% if (theme.math || page.math) { %>
    <script>
      document.addEventListener("DOMContentLoaded", function() {
        renderMathInElement(document.body, {
          delimiters: [
            {left: "$$", right: "$$", display: true},
            {left: "$", right: "$", display: false}
          ]
        });
      });
    </script>
  <% } %>
  
  <!-- Live2D -->
  <% if (theme.live2d && theme.live2d.enable) { %>
    <script src="<%= url_for('live2d/autoload.js') %>"></script>
  <% } %>
</body>
</html>